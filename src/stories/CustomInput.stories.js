import React from "react";

import CustomInput from "../components/CustomInput/CustomInput2";

export default {
    title: "Codi/CustomInput",
    component: CustomInput,
    argTypes: {
    },
};

const Template = () => <CustomInput />;

export const Input = Template.bind({});